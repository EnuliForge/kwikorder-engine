{
  "name": "@kwik/order-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "prepare": "pnpm run build"
  },
  "dependencies": {
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}